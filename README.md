# Universal Data Processing Pipeline (UDPP)

This is a simple, extensible ML pipeline for structured datasets (classification & regression).
Drop your CSV/Excel files into the `data/` folder and run `python -m src.main --data data/your.csv --target target_column`.

See `src/` for code. Output saved under `udpp_output/<dataset-name>/`.
